AGENTS.md 자가보완 프로토콜 적용. 아래 작업 수행.

=== 0. AGENTS.md에 "절대 규칙" 섹션 추가 ===

## 절대 규칙 (바꾸지 말 것)
- 모바일 브레이크포인트: 640px 유지 (아이폰 12에서 검증됨)
- 원본 코드의 상수/설정값 임의 변경 금지
- 명시적 지시 없이 "개선" 하지 말 것

=== 1. 브레이크포인트 복원 ===

main.css의 모든 @media 쿼리 확인.
768px로 된 부분이 있으면 전부 640px로 복원.

=== 2. 버그 수정: 모바일 검색 후 레이아웃 깨짐 ===

증상:
- 모바일(640px 이하)에서 검색 실행 시
- 검색창(.search-wrap)이 좌측에 세로로 쌓임
- "비/스/포/크" 한 글자씩 세로 배치됨

진단: body.search-mode 상태에서 모바일 레이아웃 규칙 누락
수정: 검색창은 상단 유지, 결과는 아래로 세로 정렬

=== 3. 버그 수정: PC 검색 중간 단계 멈춤 ===

증상:
- "검색패턴 관찰", "데이터 수집"까지 진행 후 멈춤
- 또는 "검색 중 오류 발생" 표시

진단: requestIntentInfer 또는 buildCandidates 실패
수정: 모든 외부 API 호출에 Fallback + timeout 적용

=== 필수 검증 (반드시 전부 수행) ===

1. DevTools 열고 Console 탭 활성화 유지
2. PC에서 "비스포크 AI 콤보" 검색:
   - Console 에러 0개 (스크린샷)
   - 결과 카드 정상 표시 (스크린샷)
3. DevTools 모바일 시뮬레이션 (iPhone 12, 390px):
   - 가로 스크롤 없음
   - 검색 시 검색창 상단 유지
   - 글자 세로 쌓임 없음
   - 결과 카드 세로 정렬
   - Console 에러 0개 (스크린샷)
4. Network 탭에서 실패한 요청 확인 후 리포트

모든 검증 통과 후에만 git commit + push.
검증 스킵 절대 금지.

커밋 메시지: "fix: restore 640px breakpoint + mobile search-mode + intent fallback"

완료 보고 제출물:
- Console 스크린샷 (PC + 모바일)
- 수정한 파일 목록
- 각 파일의 변경 사유
- AGENTS.md 업데이트 확인
## 검증 엄격 규칙 (절대 어기지 말 것)

1. "완료" 선언 전 다음을 모두 만족해야 함:
   - 동일 작업을 최소 3회 반복 테스트
   - 3회 모두 동일한 성공 결과
   - Vercel 로그에 에러 0개 확인
   - Console 에러 0개 확인

2. 간헐적 성공은 실패로 간주한다.
   - 1회 성공 + 2회 실패 = 실패
   - 3회 연속 성공해야만 통과

3. 수정 전 반드시:
   - 원인을 구체적으로 특정 (추측 금지)
   - 수정 계획을 텍스트로 명시
   - 사용자 확인 후 실행

4. 금지 행동:
   - "대략 해결됐습니다" 표현 금지
   - 근거 없는 "성공" 선언 금지
   - 한두 번 테스트로 판단 종결 금지
   - 코드 변경과 무관한 파일 건드리기 금지